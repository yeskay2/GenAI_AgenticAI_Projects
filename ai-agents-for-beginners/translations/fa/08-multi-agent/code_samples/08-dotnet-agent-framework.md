<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "5f5a5f2902f32272257506d88d3c43a1",
  "translation_date": "2025-11-07T09:13:42+00:00",
  "source_file": "08-multi-agent/code_samples/08-dotnet-agent-framework.md",
  "language_code": "fa"
}
-->
# 🤝 سیستم‌های گردش کار چندعاملی سازمانی (.NET)

## 📋 اهداف آموزشی

این دفترچه نحوه ساخت سیستم‌های چندعاملی پیشرفته در سطح سازمانی را با استفاده از Microsoft Agent Framework در .NET و مدل‌های GitHub نشان می‌دهد. شما یاد خواهید گرفت که چگونه چندین عامل تخصصی را از طریق گردش کارهای ساختاریافته هماهنگ کنید و از ویژگی‌های سازمانی .NET برای راه‌حل‌های آماده تولید بهره ببرید.

**قابلیت‌های چندعاملی سازمانی که خواهید ساخت:**
- 👥 **همکاری عامل‌ها**: هماهنگی نوع-ایمن عامل‌ها با اعتبارسنجی در زمان کامپایل
- 🔄 **ارکستراسیون گردش کار**: تعریف گردش کار به صورت اعلامی با الگوهای async در .NET
- 🎭 **تخصص نقش‌ها**: شخصیت‌ها و حوزه‌های تخصصی عامل‌ها با نوع قوی
- 🏢 **یکپارچه‌سازی سازمانی**: الگوهای آماده تولید با نظارت و مدیریت خطا

## ⚙️ پیش‌نیازها و تنظیمات

**محیط توسعه:**
- .NET 9.0 SDK یا بالاتر
- Visual Studio 2022 یا VS Code با افزونه C#
- اشتراک Azure (برای عامل‌های پایدار)

**پکیج‌های مورد نیاز NuGet:**
```xml
<PackageReference Include="Microsoft.Extensions.AI.Abstractions" Version="9.9.0" />
<PackageReference Include="Azure.AI.Agents.Persistent" Version="1.2.0-beta.4" />
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />
<PackageReference Include="Microsoft.Extensions.AI" Version="9.8.0" />
<PackageReference Include="DotNetEnv" Version="3.1.1" />
<PackageReference Include="Microsoft.Extensions.AI.OpenAI" Version="9.9.0-preview.1.25458.4" />
```

## نمونه کد

کد کامل این درس در فایل C# همراه موجود است: [`08-dotnet-agent-framework.cs`](../../../../08-multi-agent/code_samples/08-dotnet-agent-framework.cs)

برای اجرای نمونه:

```bash
# Make the file executable (Linux/macOS)
chmod +x 08-dotnet-agent-framework.cs

# Run the sample
./08-dotnet-agent-framework.cs
```

یا با استفاده از CLI .NET:

```bash
dotnet run 08-dotnet-agent-framework.cs
```

## آنچه این نمونه نشان می‌دهد

این سیستم گردش کار چندعاملی یک سرویس توصیه سفر هتل ایجاد می‌کند که شامل دو عامل تخصصی است:

1. **عامل FrontDesk**: یک عامل سفر که توصیه‌هایی برای فعالیت‌ها و مکان‌ها ارائه می‌دهد
2. **عامل Concierge**: توصیه‌ها را بررسی می‌کند تا تجربیات اصیل و غیرتوریستی را تضمین کند

عامل‌ها در یک گردش کار با هم همکاری می‌کنند که در آن:
- عامل FrontDesk درخواست اولیه سفر را دریافت می‌کند
- عامل Concierge توصیه‌ها را بررسی و اصلاح می‌کند
- گردش کار پاسخ‌ها را به صورت بلادرنگ پخش می‌کند

## مفاهیم کلیدی

### هماهنگی عامل‌ها
نمونه هماهنگی نوع-ایمن عامل‌ها را با استفاده از Microsoft Agent Framework و اعتبارسنجی در زمان کامپایل نشان می‌دهد.

### ارکستراسیون گردش کار
از تعریف گردش کار به صورت اعلامی با الگوهای async در .NET برای اتصال چندین عامل در یک خط لوله استفاده می‌کند.

### پخش پاسخ‌ها
پخش بلادرنگ پاسخ‌های عامل‌ها را با استفاده از async enumerables و معماری مبتنی بر رویداد پیاده‌سازی می‌کند.

### یکپارچه‌سازی سازمانی
الگوهای آماده تولید شامل موارد زیر را نشان می‌دهد:
- پیکربندی متغیرهای محیطی
- مدیریت امن اعتبارنامه‌ها
- مدیریت خطا
- پردازش رویدادهای غیرهمزمان

---

**سلب مسئولیت**:  
این سند با استفاده از سرویس ترجمه هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما تلاش می‌کنیم دقت را حفظ کنیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است شامل خطاها یا نادرستی‌ها باشند. سند اصلی به زبان اصلی آن باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، ترجمه حرفه‌ای انسانی توصیه می‌شود. ما مسئولیتی در قبال سوء تفاهم‌ها یا تفسیرهای نادرست ناشی از استفاده از این ترجمه نداریم.