# AGENTS.md

## مرور کلی پروژه

این مخزن شامل "عامل‌های هوش مصنوعی برای مبتدیان" است - یک دوره آموزشی جامع که همه چیزهای لازم برای ساخت عامل‌های هوش مصنوعی را آموزش می‌دهد. این دوره شامل بیش از ۱۵ درس است که مباحث پایه‌ای، الگوهای طراحی، چارچوب‌ها و استقرار در محیط تولید عامل‌های هوش مصنوعی را پوشش می‌دهد.

**تکنولوژی‌های کلیدی:**
- پایتون ۳٫۱۲ به بالا
- دفترچه‌های Jupyter برای یادگیری تعاملی
- چارچوب‌های هوش مصنوعی: Microsoft Agent Framework (MAF)
- خدمات هوش مصنوعی Azure: Microsoft Foundry، Azure AI Foundry Agent Service V2

**معماری:**
- ساختار مبتنی بر درس (شاخه‌های ۰۰ تا ۱۵+)
- هر درس شامل: مستندات README، نمونه‌های کد (دفترچه‌های Jupyter)، و تصاویر
- پشتیبانی چندزبانه از طریق سیستم ترجمه خودکار
- یک دفترچه پایتون برای هر درس با استفاده از Microsoft Agent Framework

## فرمان‌های راه‌اندازی

### پیش‌نیازها
- پایتون ۳٫۱۲ یا بالاتر
- اشتراک Azure (برای Azure AI Foundry)
- نصب و احراز هویت Azure CLI (`az login`)

### راه‌اندازی اولیه

1. **کلون یا فورک مخزن:**
   ```bash
   gh repo fork microsoft/ai-agents-for-beginners --clone
   # یا
   git clone https://github.com/microsoft/ai-agents-for-beginners.git
   cd ai-agents-for-beginners
   ```

2. **ایجاد و فعال کردن محیط مجازی پایتون:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # در ویندوز: venv\Scripts\activate
   ```

3. **نصب وابستگی‌ها:**
   ```bash
   pip install -r requirements.txt
   ```

4. **تنظیم متغیرهای محیطی:**
   ```bash
   cp .env.example .env
   # فایل .env را با کلیدهای API و نقاط پایانی خود ویرایش کنید
   ```

### متغیرهای محیطی مورد نیاز

برای **Azure AI Foundry** (الزامی):
- `AZURE_AI_PROJECT_ENDPOINT` - نقطه انتهایی پروژه Azure AI Foundry
- `AZURE_AI_MODEL_DEPLOYMENT_NAME` - نام استقرار مدل (مثلاً gpt-4o)

برای **Azure AI Search** (درس ۰۵ - RAG):
- `AZURE_SEARCH_SERVICE_ENDPOINT` - نقطه انتهایی جستجوی Azure AI
- `AZURE_SEARCH_API_KEY` - کلید API جستجوی Azure AI

احراز هویت: قبل از اجرای دفترچه‌ها `az login` را اجرا کنید (از `AzureCliCredential` استفاده می‌کند).

## روند توسعه

### اجرای دفترچه‌های Jupyter

هر درس شامل چندین دفترچه Jupyter برای چارچوب‌های مختلف است:

1. **راه‌اندازی Jupyter:**
   ```bash
   jupyter notebook
   ```

2. **رفتن به شاخه درس** (مثلاً `01-intro-to-ai-agents/code_samples/`)

3. **باز کردن و اجرای دفترچه‌ها:**
   - `*-python-agent-framework.ipynb` - استفاده از Microsoft Agent Framework (پایتون)
   - `*-dotnet-agent-framework.ipynb` - استفاده از Microsoft Agent Framework (.NET)

### کار با Microsoft Agent Framework

**Microsoft Agent Framework + Azure AI Foundry:**
- نیازمند اشتراک Azure است
- از `AzureAIProjectAgentProvider` برای Agent Service V2 استفاده می‌کند (عامل‌ها در پرتال Foundry قابل مشاهده‌اند)
- آماده تولید با قابلیت مشاهده‌پذیری ساخته شده در آن
- الگوی نامگذاری فایل: `*-python-agent-framework.ipynb`

## دستورالعمل‌های تست

این یک مخزن آموزشی با کد نمونه است و کد تولیدی با تست‌های خودکار نیست. برای صحت‌سنجی راه‌اندازی و تغییراتتان:

### تست دستی

1. **تست محیط پایتون:**
   ```bash
   python --version  # باید ۳.۱۲ یا بیشتر باشد
   pip list | grep -E "(agent-framework|azure-ai|azure-identity)"
   ```

2. **تست اجرای دفترچه‌ها:**
   ```bash
   # تبدیل نوت‌بوک به اسکریپت و اجرا (بررسی واردات)
   jupyter nbconvert --to script <lesson-folder>/code_samples/<notebook>.ipynb --stdout | python
   ```

3. **بررسی متغیرهای محیطی:**
   ```bash
   python -c "import os; from dotenv import load_dotenv; load_dotenv(); print('✓ GITHUB_TOKEN' if os.getenv('GITHUB_TOKEN') else '✗ GITHUB_TOKEN missing')"
   ```

### اجرای دفترچه‌های جداگانه

دفترچه‌ها را در Jupyter باز کنید و سلول‌ها را به ترتیب اجرا نمایید. هر دفترچه مستقل است و شامل:
- دستورات import
- بارگذاری تنظیمات
- پیاده‌سازی نمونه عامل‌ها
- خروجی‌های مورد انتظار در سلول‌های markdown

## سبک کد

### قراردادهای پایتون

- **نسخه پایتون:** ۳٫۱۲ به بالا
- **سبک کد:** پیروی از قوانین استاندارد PEP 8 برای پایتون
- **دفترچه‌ها:** استفاده از سلول‌های markdown روشن برای توضیح مفاهیم
- **واردات:** گروه‌بندی بر اساس کتابخانه استاندارد، کتابخانه‌های شخص ثالث، واردات محلی

### قراردادهای Jupyter Notebook

- شامل سلول‌های markdown توصیفی قبل از سلول‌های کد
- اضافه کردن نمونه‌های خروجی در دفترچه برای مرجع
- استفاده از نام‌های متغیر روشن که با مفاهیم درس مطابقت دارند
- حفظ ترتیب اجرای خطی دفترچه‌ها (سلول ۱ → ۲ → ۳...)

### سازماندهی فایل‌ها

```
<lesson-number>-<lesson-name>/
├── README.md                     # Lesson documentation
├── code_samples/
│   ├── <number>-python-agent-framework.ipynb
│   └── <number>-dotnet-agent-framework.ipynb  (optional)
└── images/
    └── *.png
```

## ساخت و استقرار

### ساخت مستندات

این مخزن از Markdown برای مستندسازی استفاده می‌کند:
- فایل‌های README.md در هر پوشه درس
- README.md اصلی در ریشه مخزن
- سیستم ترجمه خودکار از طریق GitHub Actions

### خط لوله CI/CD

در شاخه `.github/workflows/` قرار دارد:

1. **co-op-translator.yml** - ترجمه خودکار به بیش از ۵۰ زبان
2. **welcome-issue.yml** - خوش‌آمدگویی به سازندگان مسئله جدید
3. **welcome-pr.yml** - خوش‌آمدگویی به مشارکت‌کنندگان درخواست کشش

### استقرار

این یک مخزن آموزشی است - فرایند استقراری ندارد. کاربران:
1. مخزن را فورک یا کلون می‌کنند
2. دفترچه‌ها را به صورت محلی یا در GitHub Codespaces اجرا می‌کنند
3. با تغییر دادن و آزمایش نمونه‌ها یاد می‌گیرند

## دستورالعمل‌های درخواست کشش

### قبل از ارسال

1. **تست تغییرات خود:**
   - دفترچه‌های مرتبط را به طور کامل اجرا کنید
   - اطمینان حاصل کنید همه سلول‌ها بدون خطا اجرا می‌شوند
   - خروجی‌ها را بررسی کنید که مناسب باشند

2. **به‌روزرسانی مستندات:**
   - اگر مفهوم جدیدی اضافه کرده‌اید README.md را به‌روزرسانی کنید
   - در دفترچه‌ها برای کدهای پیچیده کامنت اضافه کنید
   - مطمئن شوید سلول‌های markdown هدف را به خوبی توضیح می‌دهند

3. **تغییرات فایل‌ها:**
   - از اضافه کردن فایل‌های `.env` خودداری کنید (از `.env.example` استفاده شود)
   - پوشه‌های `venv/` یا `__pycache__/` را کامیت نکنید
   - خروجی دفترچه‌ها را نگه دارید اگر مفاهیم را نشان می‌دهند
   - فایل‌های موقتی و پشتیبان دفترچه‌ها (`*-backup.ipynb`) را حذف کنید

### قالب عنوان PR

از عناوین توصیفی استفاده کنید:
- `[درس-XX] اضافه کردن نمونه جدید برای <مفهوم>`
- `[اصلاح] اصلاح اشتباه نوشتاری در README درس-XX`
- `[به‌روزرسانی] بهبود نمونه کد در درس-XX`
- `[مستندات] به‌روزرسانی دستورالعمل‌های راه‌اندازی`

### بررسی‌های اجباری

- دفترچه‌ها باید بدون خطا اجرا شوند
- فایل‌های README باید واضح و دقیق باشند
- از الگوهای کد موجود در مخزن پیروی کنید
- سازگاری با سایر دروس حفظ شود

## نکات اضافی

### مشکلات رایج

1. **عدم تطابق نسخه پایتون:**
   - اطمینان دهید از پایتون ۳٫۱۲ و به بالا استفاده می‌کنید
   - برخی کتابخانه‌ها با نسخه‌های قدیمی‌تر کار نمی‌کنند
   - برای مشخص کردن نسخه پایتون از `python3 -m venv` استفاده کنید

2. **متغیرهای محیطی:**
   - همیشه `.env` را از `.env.example` بسازید
   - فایل `.env` را کامیت نکنید (در `.gitignore` قرار دارد)
   - توکن گیت‌هاب باید دسترسی‌های لازم را داشته باشد

3. **تداخل بسته‌ها:**
   - از یک محیط مجازی تازه استفاده کنید
   - پکیج‌ها را از `requirements.txt` نصب کنید، نه جداگانه
   - برخی دفترچه‌ها ممکن است به بسته‌های اضافی ذکرشده در سلول‌های markdown نیاز داشته باشند

4. **خدمات Azure:**
   - خدمات هوش مصنوعی Azure نیاز به اشتراک فعال دارند
   - برخی قابلیت‌ها منطقه‌ای هستند
   - محدودیت‌های لایه رایگان در GitHub Models اعمال می‌شود

### مسیر یادگیری

پیشنهاد پیشرفت در دروس:
1. **۰۰-course-setup** - شروع برای راه‌اندازی محیط
2. **۰۱-intro-to-ai-agents** - فهم مبانی عامل‌های هوش مصنوعی
3. **۰۲-explore-agentic-frameworks** - یادگیری درباره چارچوب‌های مختلف
4. **۰۳-agentic-design-patterns** - الگوهای طراحی پایه
5. ادامه به ترتیب دروس شماره‌گذاری شده

### انتخاب چارچوب

چارچوب مناسب بر اساس اهداف خود انتخاب کنید:
- **همه دروس**: Microsoft Agent Framework (MAF) با `AzureAIProjectAgentProvider`
- **عامل‌ها به صورت سرور-ساید ثبت می‌شوند** در Azure AI Foundry Agent Service V2 و در پرتال Foundry قابل مشاهده‌اند

### دریافت کمک

- به [Microsoft Foundry Community Discord](https://aka.ms/ai-agents/discord) بپیوندید
- فایل‌های README هر درس را برای راهنمایی‌های خاص مطالعه کنید
- به [README.md](./README.md) اصلی برای مرور کلی دوره مراجعه کنید
- به [Course Setup](./00-course-setup/README.md) برای دستورالعمل‌های دقیق راه‌اندازی مراجعه کنید

### مشارکت

این یک پروژه آموزشی آزاد است. مشارکت‌ها استقبال می‌شود:
- بهبود نمونه‌های کد
- اصلاح اشتباهات نوشتاری یا خطاها
- افزودن کامنت‌های توضیحی
- پیشنهاد موضوعات درس جدید
- ترجمه به زبان‌های دیگر

برای نیازهای جاری به [GitHub Issues](https://github.com/microsoft/ai-agents-for-beginners/issues) مراجعه کنید.

## زمینه خاص پروژه

### پشتیبانی چندزبانه

این مخزن از سیستم ترجمه خودکار استفاده می‌کند:
- پشتیبانی از بیش از ۵۰ زبان
- ترجمه‌ها در شاخه‌های `/translations/<lang-code>/` قرار دارند
- جریان کاری GitHub Actions به‌روزرسانی ترجمه‌ها را مدیریت می‌کند
- فایل‌های منبع به زبان انگلیسی در ریشه مخزن هستند

### ساختار درس‌ها

هر درس از الگویی یکسان پیروی می‌کند:
1. تصویر بندانگشتی ویدئو به همراه لینک
2. محتوای نوشتاری درس (README.md)
3. نمونه‌های کد در چارچوب‌های مختلف
4. اهداف یادگیری و پیش‌نیازها
5. منابع یادگیری اضافی لینک شده

### نام‌گذاری نمونه کدها

فرمت: `<lesson-number>-python-agent-framework.ipynb`
- `01-python-agent-framework.ipynb` - درس ۱، چارچوب MAF پایتون
- `14-sequential.ipynb` - درس ۱۴، الگوهای پیشرفته MAF

### شاخه‌های خاص

- `translated_images/` - تصاویر محلی‌سازی شده برای ترجمه‌ها
- `images/` - تصاویر اصلی برای محتوای انگلیسی
- `.devcontainer/` - پیکربندی کانتینر توسعه VS Code
- `.github/` - جریان‌های کاری و قالب‌های GitHub Actions

### وابستگی‌ها

پکیج‌های کلیدی در `requirements.txt`:
- `agent-framework` - چارچوب Microsoft Agent
- `a2a-sdk` - پشتیبانی پروتکل عامل به عامل
- `azure-ai-inference`، `azure-ai-projects` - خدمات هوش مصنوعی Azure
- `azure-identity` - احراز هویت Azure (AzureCliCredential)
- `azure-search-documents` - ادغام با Azure AI Search
- `mcp[cli]` - پشتیبانی پروتکل متن مدل

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**توجه**:  
این سند با استفاده از سرویس ترجمه هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما در تلاش برای دقت هستیم، لطفاً آگاه باشید که ترجمه‌های خودکار ممکن است دارای خطا یا نادرستی باشند. سند اصلی به زبان اصلی خود باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، استفاده از ترجمه حرفه‌ای انسانی توصیه می‌شود. ما مسئول هیچ گونه سوءتفاهم یا تفسیر نادرستی که ناشی از استفاده از این ترجمه باشد، نیستیم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->